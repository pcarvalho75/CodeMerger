<Window x:Class="CodeMerger.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="promptText" Text="Enter value:" Margin="0,0,0,8"/>
        <TextBox x:Name="responseTextBox" Grid.Row="1" Padding="5" BorderBrush="#CCC"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="75" Padding="5" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="OK" Width="75" Padding="5" IsDefault="True" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
