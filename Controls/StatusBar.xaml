<UserControl x:Class="CodeMerger.Controls.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#12121f" Padding="24,12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="statusText" Text="Ready" Foreground="{StaticResource TextSecondary}" 
                       TextWrapping="Wrap" VerticalAlignment="Center" FontSize="12"/>

            <TextBlock x:Name="fileStatusLabel" Grid.Column="1" FontStyle="Italic" Foreground="{StaticResource TextSecondary}" 
                       VerticalAlignment="Center" Margin="0,0,16,0" FontSize="11"/>

            <ProgressBar x:Name="progressBar" Grid.Column="2" Visibility="Collapsed" Width="200" Height="6" 
                         VerticalAlignment="Center" Background="{StaticResource BackgroundCard}" 
                         Foreground="{StaticResource AccentPrimary}"/>
        </Grid>
    </Border>
</UserControl>
