<Window x:Class="CodeMerger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CodeMerger" Height="800" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">
    <Window.Resources>
        <!-- Modern Dark Theme Colors -->
        <SolidColorBrush x:Key="BackgroundDark" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="BackgroundCard" Color="#16213e"/>
        <SolidColorBrush x:Key="BackgroundCardHover" Color="#1f2b47"/>
        <SolidColorBrush x:Key="AccentPrimary" Color="#e94560"/>
        <SolidColorBrush x:Key="AccentSecondary" Color="#0f3460"/>
        <SolidColorBrush x:Key="AccentSuccess" Color="#00d9a5"/>
        <SolidColorBrush x:Key="AccentWarning" Color="#ffc107"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#eaeaea"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8892a0"/>
        <SolidColorBrush x:Key="BorderColor" Color="#2a3a5a"/>

        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="{StaticResource AccentSecondary}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a4a7a"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0a2a4a"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#d63050"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#b82545"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="LinkButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentPrimary}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#ff6b8a"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SmallLinkButton" BasedOn="{StaticResource LinkButton}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>

        <Style TargetType="Button" x:Key="IconButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundCardHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="GitButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#6e5494"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#8a6aae"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#5a4378"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundDark}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Toggle Button -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="6"
                        Background="{StaticResource BackgroundDark}"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1"/>
                <Path x:Name="Arrow" Grid.Column="1" Fill="{StaticResource TextSecondary}"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="ComboBox" x:Key="ModernComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundDark}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}"
                                          Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,8,28,8" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{StaticResource BackgroundCard}"
                                            BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="6"/>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundCardHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox" x:Key="ModernListBox">
            <Setter Property="Background" Value="{StaticResource BackgroundDark}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <!-- Keep old button styles for compatibility but hide them -->
        <Style TargetType="Button" x:Key="McpButton" BasedOn="{StaticResource PrimaryButton}"/>
        <Style TargetType="Button" x:Key="McpStopButton" BasedOn="{StaticResource PrimaryButton}"/>
    </Window.Resources>

    <Grid x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER BAR -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundCard}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                    <Border Background="{StaticResource AccentPrimary}" CornerRadius="8" Padding="8" Margin="0,0,12,0">
                        <Path Data="M 12,3 L 20,7.5 L 20,16.5 L 12,21 L 4,16.5 L 4,7.5 Z M 12,8 L 8,10.5 L 8,15.5 L 12,18 L 16,15.5 L 16,10.5 Z" 
                              Fill="White" Stretch="Uniform" Width="20" Height="20"/>
                    </Border>
                    <TextBlock Text="CodeMerger" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Project Selection -->
                <TextBlock Grid.Column="1" Text="Project" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="13"/>
                <ComboBox x:Name="projectComboBox" Grid.Column="2" DisplayMemberPath="Name" VerticalAlignment="Center" 
                          SelectionChanged="ProjectComboBox_SelectionChanged" Style="{StaticResource ModernComboBox}"/>
                
                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="12,0,0,0">
                    <Button Content="＋ New" Click="NewProject_Click" Style="{StaticResource ModernButton}" Margin="0,0,6,0"/>
                    <Button Content="✎ Rename" Click="RenameProject_Click" Style="{StaticResource ModernButton}" Margin="0,0,6,0"/>
                    <Button Content="🗑 Delete" Click="DeleteProject_Click" Style="{StaticResource ModernButton}"/>
                </StackPanel>

                <!-- Connection Status -->
                <Border Grid.Column="5" Background="{StaticResource BackgroundDark}" CornerRadius="20" Padding="16,8" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse x:Name="connectionIndicator" Width="10" Height="10" Fill="#555" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="connectionStatusText" Text="Waiting for Claude..." Foreground="{StaticResource TextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                        <Button x:Name="stopServerButton" Content="⏹ Stop" Click="StopServer_Click" 
                                Style="{StaticResource SmallLinkButton}" Margin="12,0,0,0" Visibility="Collapsed"
                                ToolTip="Stop MCP server to allow recompilation"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Info Banner -->
        <Border x:Name="recommendationBanner" Grid.Row="1" Background="#2a2a4a" Padding="16,12" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="💡" FontSize="16" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBlock x:Name="recommendationText" Grid.Column="1" TextWrapping="Wrap" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- MAIN CONTENT -->
        <Grid Grid.Row="2" Margin="24,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL -->
            <Grid Grid.Column="0" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Input Directories Card -->
                <Border Grid.Row="0" Background="{StaticResource BackgroundCard}" CornerRadius="12" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="📁 Source Directories" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                            <TextBlock x:Name="directoryCountText" Grid.Column="1" Text="" FontSize="12" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Content="All" Click="SelectAllDirectories_Click" Style="{StaticResource SmallLinkButton}" ToolTip="Select all"/>
                                <TextBlock Text="·" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="6,0"/>
                                <Button Content="None" Click="DeselectAllDirectories_Click" Style="{StaticResource SmallLinkButton}" ToolTip="Deselect all"/>
                            </StackPanel>
                        </Grid>
                        
                        <ListBox x:Name="inputDirListBox" Grid.Row="1" Margin="0,12,0,12" Style="{StaticResource ModernListBox}" SelectionMode="Extended">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="4">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                                        <Setter Property="Opacity" Value="0.4"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                                  Checked="DirectoryCheckBox_Changed" 
                                                  Unchecked="DirectoryCheckBox_Changed"/>
                                        <TextBlock Text="{Binding Path}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimary}" FontSize="12">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Remove" Click="RemoveDirectory_Click" Margin="0,0,8,0" Style="{StaticResource ModernButton}"/>
                            <Button Content="+ Add Folder" Click="AddDirectory_Click" Style="{StaticResource PrimaryButton}"/>
                        </StackPanel>

                        <Grid Grid.Row="3" Margin="0,20,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Extensions" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                            <TextBox x:Name="extensionsTextBox" Grid.Row="1" Text=".cs, .xaml, .py" Margin="0,6,0,12" 
                                     TextChanged="Filters_Changed" Style="{StaticResource ModernTextBox}"/>
                            <TextBlock Grid.Row="2" Text="Ignored Directories" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}"/>
                            <TextBox x:Name="ignoredDirsTextBox" Grid.Row="3" Text="bin, obj, .vs, Properties" Margin="0,6,0,0" 
                                     TextChanged="Filters_Changed" Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- External Repositories Card -->
                <Border Grid.Row="1" Background="{StaticResource BackgroundCard}" CornerRadius="12" Padding="20" Margin="0,12,0,0">
                    <Expander IsExpanded="False" Foreground="{StaticResource TextPrimary}">
                        <Expander.Header>
                            <TextBlock Text="🔗 External Git Repositories" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                        </Expander.Header>
                        <StackPanel Margin="0,16,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="gitUrlTextBox" Style="{StaticResource ModernTextBox}"
                                         Text="https://github.com/user/repo"/>
                                <Button Grid.Column="1" Content="Clone" Click="AddGitRepo_Click" 
                                        Style="{StaticResource GitButton}" Margin="8,0,0,0"/>
                            </Grid>

                            <ListBox x:Name="gitRepoListBox" Margin="0,12,0,0" MinHeight="60" MaxHeight="120" Style="{StaticResource ModernListBox}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}" 
                                                      VerticalAlignment="Center" Margin="0,0,10,0"
                                                      Checked="GitRepoCheckBox_Changed" 
                                                      Unchecked="GitRepoCheckBox_Changed"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                                                <TextBlock Text="{Binding Branch}" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                                            </StackPanel>
                                            <Button Grid.Column="2" Content="🔄" Click="RefreshGitRepo_Click" 
                                                    Style="{StaticResource IconButton}" ToolTip="Pull latest"/>
                                            <Button Grid.Column="3" Content="✕" Click="RemoveGitRepo_Click" 
                                                    Style="{StaticResource IconButton}" Foreground="{StaticResource AccentPrimary}" ToolTip="Remove"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <TextBlock x:Name="gitStatusText" Text="" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Expander>
                </Border>
            </Grid>

            <!-- RIGHT PANEL: Files -->
            <Border Grid.Column="1" Margin="12,0,0,0" Background="{StaticResource BackgroundCard}" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="📄 Found Files" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                    <ListBox x:Name="fileListBox" Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource ModernListBox}" 
                             HorizontalContentAlignment="Stretch" FontSize="12"/>
                </Grid>
            </Border>
        </Grid>

        <!-- BOTTOM BAR -->
        <Border Grid.Row="3" Background="{StaticResource BackgroundCard}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Claude Desktop Status -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Claude Desktop:" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Border Background="{StaticResource BackgroundDark}" CornerRadius="4" Padding="8,4" Margin="0,0,16,0">
                        <TextBlock x:Name="claudeInstallStatus" Text="Checking..." Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                    </Border>
                    <TextBlock Text="Config:" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Border Background="{StaticResource BackgroundDark}" CornerRadius="4" Padding="8,4">
                        <TextBlock x:Name="claudeConfigStatus" Text="Checking..." Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                    </Border>
                </StackPanel>

                <!-- Output Folder -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Output:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="outputFileTextBox" Width="300" IsReadOnly="True" Style="{StaticResource ModernTextBox}" 
                             Background="{StaticResource BackgroundDark}" FontSize="11"/>
                    <Button Content="📂 Open" Click="OpenFolder_Click" Style="{StaticResource ModernButton}" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="claudeDownloadButton" Content="Download Claude" Click="ClaudeDownload_Click" Style="{StaticResource LinkButton}" Visibility="Collapsed"/>
                    <Button x:Name="claudeAddConfigButton" Content="Add to Claude" Click="ClaudeAddConfig_Click" Style="{StaticResource ModernButton}" Visibility="Collapsed" Margin="0,0,8,0"/>
                    <Button Content="Open Config" Click="ClaudeOpenConfigFolder_Click" Style="{StaticResource LinkButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- STATUS BAR -->
        <Border Grid.Row="4" Background="#12121f" Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusForeground}" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="12"/>
                
                <TextBlock x:Name="fileStatusLabel" Grid.Column="1" FontStyle="Italic" Foreground="{StaticResource TextSecondary}" 
                           VerticalAlignment="Center" Margin="0,0,16,0" FontSize="11"/>
                
                <ProgressBar x:Name="progressBar" Grid.Column="2" Visibility="Collapsed" Width="200" Height="6" 
                             VerticalAlignment="Center" Background="{StaticResource BackgroundCard}" 
                             Foreground="{StaticResource AccentPrimary}"/>
            </Grid>
        </Border>

        <!-- Hidden elements kept for compatibility -->
        <TextBlock x:Name="projectStatusText" Visibility="Collapsed"/>
        <Border x:Name="claudeDesktopPanel" Visibility="Collapsed"/>
        <Border x:Name="mcpStatusPanel" Visibility="Collapsed"/>
        <TextBlock x:Name="mcpConfigText" Visibility="Collapsed"/>
        <Button x:Name="mergeButton" Visibility="Collapsed" Click="Merge_Click"/>
        <Button x:Name="mcpButton" Visibility="Collapsed" Click="McpServer_Click"/>
    </Grid>
</Window>
